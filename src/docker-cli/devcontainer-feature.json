{
    "id": "docker-cli",
    "version": "1.0.0",
    "name": "Docker CLI",
    "description": "Installs the Docker CLI tool (client only, without the daemon).",
    "options": {
        "version": {
            "type": "string",
            "proposals": [
                "latest",
                "27.0.3",
                "27.0.2",
                "27.0.1",
                "27.0.0"
            ],
            "default": "latest",
            "description": "Select version of Docker CLI to install, if not latest."
        }
    },
    "customizations": {
        "vscode": {
            "settings": {
                "github.copilot.chat.codeGeneration.instructions": [
                    {
                        "text": "This dev container includes the Docker CLI (`docker`), which is pre-installed and available on the `PATH`."
                    }
                ]
            }
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
