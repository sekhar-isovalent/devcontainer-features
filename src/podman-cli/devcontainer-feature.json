{
    "id": "podman-cli",
    "version": "1.0.0",
    "name": "Podman CLI",
    "description": "Installs the Podman CLI tool (client only, without the daemon).",
    "options": {
        "version": {
            "type": "string",
            "proposals": [
                "latest",
                "5.1.0",
                "5.0.0",
                "4.9.0",
                "4.8.0"
            ],
            "default": "latest",
            "description": "Select version of Podman CLI to install, if not latest."
        }
    },
    "customizations": {
        "vscode": {
            "settings": {
                "github.copilot.chat.codeGeneration.instructions": [
                    {
                        "text": "This dev container includes the Podman CLI (`podman`), which is pre-installed and available on the `PATH`."
                    }
                ]
            }
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
